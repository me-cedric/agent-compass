---
description: Agent Compass contract and mission router
alwaysApply: true
---

Read `AGENTS.md` first. It is the canonical agent-compass contract.

Asked to set up an existing project, bootstrap a new one, or add a capability
to compass? Route through `MISSIONS.md` and follow the matching
`skills/compass-*` playbook.

Host project `AGENTS.md` takes precedence when it adds project-specific rules.
Do not commit, push, deploy, publish, or open PRs unless explicitly asked.
