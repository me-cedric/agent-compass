{
  "schema": 1,
  "name": "regulated-api",
  "description": "API-heavy setup with contract, security, traceability, and validation gates.",
  "gates": [
    "provider-verify --strict",
    "quality-gates --strict",
    "spec-validation-map --strict",
    "mcp-probe",
    "redact",
    "agent-conformance --strict"
  ],
  "defaults": {
    "skills": "copy",
    "specKit": true,
    "dashboard": true,
    "taskLog": true,
    "apiContractSync": true
  }
}
